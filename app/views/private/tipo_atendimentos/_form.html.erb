<%= form_with(model: tipo_atendimento, local: true) do |form| %>
  <% if tipo_atendimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_atendimento.errors.count, "error") %> prohibited this tipo_atendimento from being saved:</h2>

      <ul>
        <% tipo_atendimento.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
