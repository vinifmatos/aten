<%= form_with(model: assunto, local: true) do |form| %>
  <% if assunto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assunto.errors.count, "error") %> prohibited this assunto from being saved:</h2>

      <ul>
        <% assunto.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descricao %>
    <%= form.text_field :descricao %>
  </div>

  <div class="field">
    <%= form.label :tipo_tendimento_id %>
    <%= form.text_field :tipo_tendimento_id %>
  </div>

  <div class="field">
    <%= form.label :ativo %>
    <%= form.check_box :ativo %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
